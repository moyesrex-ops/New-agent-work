{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "regions": ["cpt1"],
  "buildCommand": "npx tsx scripts/migrate.mts && next build",
  "crons": [
    {
      "path": "/api/cron/retry",
      "schedule": "*/5 * * * *"
    }
  ]
}
